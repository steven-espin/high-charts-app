import React from 'react';

import classes from './HighChart.css';

const highChart = (props) => {
    return (
        <div className={classes.HighChart}>
            
        </div>
    );
}

export default highChart;